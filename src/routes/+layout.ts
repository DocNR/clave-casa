// Single-page app — render entirely client-side. localStorage and
// crypto live in the browser, no server-side rendering needed.
export const ssr = false;
export const prerender = false;
export const trailingSlash = 'never';
